#pragma once
#include<vector>
using std::vector;

int findContentChildren(vector<int>& g, vector<int>& s);			//455. Assign Cookies
int candy(vector<int>& ratings);									//135. Candy
int eraseOverlapIntervals(vector<vector<int>>& intervals);			//435. Non-overlapping Intervals
bool canPlaceFlowers(vector<int>& flowerbed, int n);				//605. Can Place Flowers
int findMinArrowShots(vector<vector<int>>& points);					//452. Minimum Number of Arrows to Burst Balloons
vector<int> partitionLabels(string S);								//763. Partition Labels
int maxProfitII(vector<int>& prices);								//122. Best Time to Buy and Sell Stock II
vector<vector<int>> reconstructQueue(vector<vector<int>>& people);	//406. Queue Reconstruction by Height
bool checkPossibility(vector<int>& nums);							//665. Non-decreasing Array
